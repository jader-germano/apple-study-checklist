# API Docs

- Scope: `feature` and `component`
- Category: `api`

Use this area for app-facing contracts and module behavior that developers need to integrate with, extend, or test.

Current documents:

- `vault-workspace-api.md`: markdown vault source, loading, editability, and UI-facing state
